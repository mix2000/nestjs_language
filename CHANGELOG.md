# Changelog

## [0.0.10] - 2025-11-28

### 🐛 Исправлено
- **Критическое исправление**: Модуль теперь использует ленивую загрузку (lazy loading) для TypeORM и Sequelize
- Исправлена ошибка `Cannot find module '@nestjs/typeorm'` при использовании только Sequelize
- Исправлена ошибка `Cannot find module '@nestjs/sequelize'` при использовании только TypeORM
- Теперь загружаются только те зависимости, которые реально используются

### 🔧 Технические изменения
- Заменены статические импорты на динамические `require()` с проверкой наличия зависимостей
- Добавлены информативные сообщения об ошибках при отсутствии нужных пакетов

## [0.0.9] - 2025-11-28
_Версия пропущена из-за предварительной публикации_

## [0.0.8] - 2025-11-24

### ✨ Добавлено
- **Расширенные методы для работы с переводами**:
  - `saveTranslations()` - массовое сохранение/обновление переводов (upsert)
  - `deleteTranslations()` - удаление переводов для элемента с опциональным fieldName
  - `deleteTranslationsBatch()` - пакетное удаление переводов для нескольких элементов
- Новый интерфейс `ITranslationData` для упрощенной работы с данными переводов
- Реализация новых методов для обоих ORM (TypeORM и Sequelize)

### 🔧 Улучшения
- Упрощена работа с массовыми операциями над переводами
- Добавлены примеры использования новых методов в README

## [0.0.7] - 2025-11-24

### ✨ Добавлено
- **Поддержка Sequelize-TypeScript** - модуль теперь поддерживает как TypeORM, так и Sequelize
- Новые модели для Sequelize:
  - `LanguageModel` - модель языка
  - `TranslationModel` - модель перевода
- Новые сервисы для Sequelize:
  - `LanguageSequelizeService` - CRUD операции для языков
  - `TranslationSequelizeService` - CRUD операции для переводов
- Динамический модуль `LanguageModule.forRoot({ orm: 'typeorm' | 'sequelize' })`
- Инъекция через токены `LANGUAGE_SERVICE` и `TRANSLATION_SERVICE`
- Полная документация в `README.md`
- Файл с примерами использования `USAGE_EXAMPLE.md`

### 🔄 Изменено
- Интерфейсы `ILanguageService` и `ITranslationService` теперь ORM-агностичные
- `LanguageModule` преобразован из статического в динамический
- `LanguageController` теперь использует инъекцию через токен вместо прямой зависимости
- Обновлен `package.json` с опциональными peer dependencies для обоих ORM

### 🐛 Исправлено
- Исправлен порядок импортов согласно ESLint правилам
- Удален неиспользуемый импорт `Index` из `translation.model.ts`
- Исправлено форматирование кода по Prettier

### 📦 Структура проекта

```
src/
├── index.ts                                    # Главная точка входа
└── language/
    ├── entity/
    │   ├── index.ts                           # Экспорты всех моделей
    │   ├── sequelize/
    │   │   ├── language.model.ts              # Sequelize модель языка
    │   │   └── translation.model.ts           # Sequelize модель перевода
    │   └── typeorm/
    │       ├── language.entity.ts             # TypeORM entity языка
    │       └── translation.entity.ts          # TypeORM entity перевода
    ├── service/
    │   ├── index.ts                           # Экспорты всех сервисов
    │   ├── sequelize/
    │   │   ├── language-sequelize.service.ts  # Sequelize сервис для языков
    │   │   └── translation-sequelize.service.ts # Sequelize сервис для переводов
    │   └── typeorm/
    │       ├── language-typeorm.service.ts    # TypeORM сервис для языков
    │       └── translation-typeorm.service.ts # TypeORM сервис для переводов
    ├── interfaces.ts                          # Общие интерфейсы
    ├── language.controller.ts                 # REST контроллер
    ├── language.module.ts                     # Динамический модуль
    └── swagger.ts                             # Swagger декораторы
```

### 📝 Использование

#### TypeORM
```typescript
LanguageModule.forRoot({ orm: 'typeorm' })
```

#### Sequelize
```typescript
LanguageModule.forRoot({ orm: 'sequelize' })
```

### 🔧 Конфигурация

**peerDependencies (опциональные):**
- `@nestjs/typeorm` и `typeorm` - для TypeORM
- `@nestjs/sequelize`, `sequelize`, `sequelize-typescript` - для Sequelize

Пользователи могут выбрать любой ORM и установить только необходимые зависимости.

### ⚡ Breaking Changes
- `LanguageModule` теперь требует вызова `forRoot()` с параметром `orm`
- Старая статическая конфигурация больше не поддерживается

### 🎯 Миграция с предыдущей версии

**Было:**
```typescript
imports: [
    TypeOrmModule.forFeature([LanguageEntity, TranslationEntity]),
    LanguageModule,
]
```

**Стало:**
```typescript
imports: [
    LanguageModule.forRoot({ orm: 'typeorm' }),
]
```
